# This file is used to set environment variables 
# Set these to correspond with the TOC you want to inventory and the file you want to create
TOC_FILE=C:/git/azure-ai-docs-pr/articles/ai-foundry/toc.yml
URL_PATH=https://learn.microsoft.com/azure/ai-foundry/
OUTPUT_FILE=foundry-toc.csv

# Set these for the metadata extraction script
BASE_PATH=C:/git/azure-ai-docs-pr/articles/

# Set this for merging existing NextGen data
EXISTING_EXCEL_FILE=

##########################################################
#### ADVANCED CONFIGURATION FOR METADATA EXTRACTION ####
# Metadata fields to extract (comma-separated) - controls which YAML front matter fields to include
# Format: "field1,field2,field3" - each field becomes a column in output
# Example alternatives: "author,title,category,tags" or "ms.author,ms.date,ms.reviewer"
METADATA_FIELDS="ms.author,ms.topic,ms.service,description"

# NOTE: Pivot processing is automatic - if "zone_pivot_groups" is included in METADATA_FIELDS above, 
# pivot logic is enabled and pivot columns (pivot_id, has_pivots, pivot_groups) are added

# Metadata flags with special processing (comma-separated)
# Format: "field1:flag_name,field2:flag_name" - creates boolean flags based on field content
# Example: "ms.custom:hub-only" creates hub-only flag if ms.custom contains "hub-only"  
METADATA_FLAGS="ms.custom:hub-only"

# Control script behavior with True/False flags
DEBUG=True
MERGE_ENGAGEMENT=False 
MERGE_EXISTING=True

# Merging columns configuration (comma-separated)
# Format: "KeyColumn,DataColumn1,DataColumn2,DataColumn3"
# Example: "URL,Notes,NextGen?,NextGen TOC" or "Href,Status,Priority,Owner"
MERGE_COLUMNS="URL,Notes,NextGen?,NextGen TOC"

##########################################################
#### OPTIONAL SETTINGS FOR DEBUGGING INDEPENDENT STEPS ####
# Use these files when running scripts separately:
METADATA_FILE=foundry-toc.csv
METADATA_OUTPUT_FILE=foundry-toc.csv
CONTENT_FILE=foundry-toc.csv
CONTENT_OUTPUT_FILE=foundry-toc-content.csv
PIVOT_MAP_FILE=C:/git/azure-ai-docs-pr/zone-pivots/zone-pivot-groups.yml

# WIP NOT NEEDED NOW Set these for the docs agent functionality
DOCS_AGENT_ENDPOINT=https://mydemo-resource.services.ai.azure.com/api/projects/mydemo
DOCS_AGENT_ID=asst_eH8uOuJSTvNjzsjS0qsN3OoL
AGENT_OUTPUT_FILE=foundry-toc-with-agent-responses.csv
AGENT_DELAY_SECONDS=2
RESUME_PROCESSING=false
MAX_RETRIES=3
RETRY_DELAY_SECONDS=30

# WIP NOT NEEDED NOW  Set these to use an AI Foundry model to summarize files 
# Get these values from AI Foundry
DEPLOYMENT_NAME=gpt-4.1-mini
ENDPOINT_URL="add your endpoint here"
